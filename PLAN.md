# 生图响应等待排查与修复

## 当前目标

排查用户第三次请求上游显示返回、页面仍等待后被手动停止的问题。基于已验收候选 39ebf42595f6c76ffde6b53dfeca9bd9d0b4d43f 修复本地已复现的 JSON 完整但 HTTP 分块未结束等待，并补充脱敏阶段诊断。

## 进度

第三次原请求没有阶段日志，不能还原真实触发条件；未重发真实请求。保留旧版复现：标准 JSON 成功，完整 JSON 缺少 HTTP 分块结束标志时超时。候选已修复接收完成判断、SSE/HTTP 错误及时返回以及超时/取消诊断；45 项聚焦测试通过。所有版本与证据在仓库外 生图质量测试/20260915-response-investigation/handoff.json 维护。

## 下一步

固定修复候选，完成工作台全部必需检查和独立复核，再切换本机 18096 预览到同一 SHA，并为该预览配置仓库外滚动诊断日志。

## 未决事项

原第三次请求的真实响应类型和传输结束状态尚未证实。真实渠道调用、合并、推送与发布未获本次授权；原工作台目录保持既有修改边界。
