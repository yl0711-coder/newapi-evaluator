# 范围与恢复入口

目标：创建中转站 Skill，并交付五种模式的独立 Mock 实验室，固定本地功能提交后独立验收。真实环境验证尚未开始。

固定基线：本地 main 初始空提交；开发分支 feature/mock-capacity-lab。2026-09-11 用户指定并授权推送到 yl0711-coder/newapi-evaluator 的同名功能分支；保留 origin，无 PR、无合并。

## 本轮工作

- [x] 完整阅读 skill-creator、既有 evaluator 工作流及内部开发/验收模式。
- [x] 创建 relay-station 与项目 AGENTS.md、空基线和功能分支。
- [x] Skill 格式通过 quick_validate；名称、UI 名称、自动调用与两个内部模式引用已核对。
- [x] 异步 HTTP、OpenAI 兼容适配器、Mock 上游/号池/网关及故障注入。
- [x] 单号、号池、网关、长任务、故障五种模式及统一指标/脱敏报告。
- [x] 36 项自动测试通过，覆盖 Ctrl+C、跨执行器检查点恢复、敏感内容不落盘和网络边界；源码安全扫描通过。
- [x] 五模式首轮 Mock E2E 通过；最终版本与提交后独立 E2E 均需要另行留存证据。

## 提交后验收状态

为避免“更新计划又改变待验 SHA”的循环，提交后的权威状态保存在数据目录 `delivery.json`、`acceptance-<完整SHA>/acceptance.json` 和 `acceptance-report.md`，不通过修改此计划来宣称验收完成。继续时必须核对这些文件的 SHA 与实时 HEAD 相同。

独立验收会重新执行聚焦、完整、安全和五模式 E2E；验收副本必须 clean detached HEAD，使用独立离线环境。示例原始指标、summary 和 report 位于该验收目录的 examples/ 数据子目录。

范围审计同时保存既有项目指纹。如观察到其他工作区在本轮期间被外部活动改动，应记录差异并保留，不将“本任务未写入其他项目”夸大为“所有其他工作区期间完全不变”。本任务写入范围仅 Skill、开发项目、指定验收检出和运行数据目录。

## 继续时

读取实时 Git 状态、当前 HEAD、数据目录对应 SHA 的验收报告。验收失败只在开发目录修复，提交后重验。不要依靠计划复选框宣称完成。

## 验收入口

`python scripts/test_all.py` 完整自动测试；`python scripts/e2e.py --output <数据目录中的新目录>` 五种模式端到端；`python scripts/repo_security_scan.py .` 安全扫描。所有 Python 命令设置 `PYTHONDONTWRITEBYTECODE=1`；依赖环境、TMPDIR 与运行证据都放在指定数据目录。

## 持续并发改造（2026-09-10）

- 保留既有未提交控制台及凭据内存传入逻辑；修改前备份在数据目录 `sustained-development-20260910/before`。
- 单号和网关新增按时长持续补发，达到时长或请求上限后收尾，恢复探测独立串行。停止操作取消在途请求并保存部分指标。
- 新增长输出与可选输出上限参数；原短输出及五模式配置保持兼容。
- 请求指标记录起止与首段内容时间。客户端在途/接收中曲线、精确时间加权均值、满并发时间占比与收尾时长进入页面和报告；不推断真实账号生成占用。
- 覆盖持续补发、真实本地 socket 重叠、截止与收尾、取消、超时释放、请求上限提前结束、输出长度上限和旧报告重建的聚焦测试。
- 本轮只用本地 Mock 验证；最新功能提交的独立复测证据保存在 `acceptance-<SHA>`。历史真实请求不证明本轮实现已在真实上游验证。

## 固定混合批次（2026-09-10）

- 用户要求一次并发发出 2 短、2 中、6 长，观察原始请求排队或报错；结束不补发，参考上游限制 5 不作为测量结果。
- 新增共同启动信号、按档配置输出、逐条进行中时间线、短／中完成后的等待请求追踪；不追加恢复探测，不推断稳定容量。
- 新模式分开首段等待、流空闲和总时限；保存部分输出、HTTP 到达、末段和块间最大间隔，安全分类流内错误。
- Mock 新增排队／拒绝两种准入行为。9 项聚焦测试与首次完整 54 项测试通过，覆盖固定请求数、队列释放、拒绝、停止、心跳及超时、原始格式兼容。
- 源码检查及 JavaScript 语法检查通过；最终本地提交和独立 detached HEAD 验收证据仍以数据目录中对应完整 SHA 为准。
- 运行证据：数据目录 `mixed-development-20260910-fixed-burst`。本轮不追加真实接口调用。
- 源码级前端行为测试覆盖表单模式切换、10 条混合参数提交、进行中行、释放观察及指标标签；本地 HTTP API 已验证 10 条原始请求和 4 次短／中结束观察。浏览器自动化连接超时，未宣称完成截图目检。

## 三路实时负载推子（2026-09-12）

- 保留短／中／长三路，初始目标均为 0，不固定请求配比。运行中提高目标补发，降低目标让原请求自然结束；暂停只停止补发，停止测试取消在途。
- 推子接口复用本机 Host、Origin、会话校验和真实调用确认。调节仅接受三路目标和暂停状态，不接受新地址、凭据或任意配置。
- 客户端连接池按本次并发上限预留；补发间隔、最长时长和累计请求上限可配置。达到运行上限后收尾；不追加恢复探测，不宣称测定稳定容量。
- 页面和报告区分等待响应头、等待首段、接收中、完整结束及具体错误；完整请求写入 results.jsonl，调节历史写入 fader-events.json。动态目标下不套用固定目标满并发占比。
- Mock 使用实际 loopback HTTP/SSE，短／中／长默认 2／6／18 秒，虚拟容量和排队／拒绝模式用于测试调节效果。未调用真实上游。
- 聚焦测试覆盖负载中新增请求、原请求排队释放、立即拒绝、调低不取消、暂停、取消、上限收尾、HTTP 会话边界、历史载入及部分报告重建。最终 SHA 与独立验收以数据目录 delivery.json 和 acceptance-<SHA> 为准。
- 浏览器自动化连接超时且内置浏览器不可用，未完成截图目检；前端语法、交互契约和实际本地 HTTP 调节链路均有自动检查。
